// Desenvolvimento local (`ng serve`). Em produção, `angular.json` substitui este arquivo por
// `environment.prod.ts` (fileReplacements na configuration `production`).

export const environment = {
  production: false,
  // apiUrl: 'http://localhost:5218/',
  apiUrl: 'https://api.cpsadmin.com.br/',
};
